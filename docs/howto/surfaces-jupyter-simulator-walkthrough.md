# Jupyter Skeleton Walkthrough (Simulator)

1. Start the local simulator profile.
2. Use notebook helper to submit sample workload.
3. Poll status until terminal state.
4. Fetch results and capture trace metadata (`job_id`, `trace_id`, target profile).

This walkthrough validates notebook-side bootstrap flow for P8D-06.
