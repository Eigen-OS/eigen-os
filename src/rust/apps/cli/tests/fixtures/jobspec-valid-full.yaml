apiVersion: eigen.os/v0.1
kind: QuantumJob
metadata:
  name: vqe-h2
  labels:
    example: "true"
  annotations:
    owner: "dev"
spec:
  program: |
    @hybrid_program
    def main():
      return 2
  target: sim:local
  priority: 10
  compiler_options:
    optimization_level: "1"
  metadata:
    shots: "1024"
  dependencies:
    - s3://bucket/file1
    - https://example.com/file2