# Optimizer Plugin Runtime

The Rust implementation of the [Optimizer Plugin SDK v1](../../../../docs/reference/plugins/optimizer-plugin-sdk.md). It exposes a lifecycle-gated runtime, an optimizer trait, deterministic state snapshots, and the `io.eigen.optimizer.cobyla` reference plugin.
