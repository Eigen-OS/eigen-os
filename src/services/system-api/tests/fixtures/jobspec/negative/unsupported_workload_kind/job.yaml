apiVersion: eigen.os/v1
kind: QuantumJob
metadata:
  name: unsupported-workload-kind
spec:
  target: sim:local
  program:
    source: |
      @hybrid_program
      def main():
          return 1
  workload:
    kind: NotARealKind
    