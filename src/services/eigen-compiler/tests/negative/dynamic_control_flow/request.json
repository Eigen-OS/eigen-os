{
  "rpc": "CompileCircuit",
  "request": {
    "language": "eigen-lang",
    "source": "from eigen_lang import hybrid_program\n\n@hybrid_program()\ndef bad(flag):\n    if flag:\n        ry(0, theta=1.0)\n"
  },
  "expected": {
    "code": "INVALID_ARGUMENT",
    "fields": ["source"]
  }
}